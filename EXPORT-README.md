# 吴义博视频作品集

## 本地预览

```bash
npm install
npm run dev
```

打开终端显示的本地地址，通常为 `http://localhost:5173/`。

## 生产构建

```bash
npm run build
npm run preview
```

完整视频素材位于 `public/media/`。生产构建会把这些素材复制到 `dist/`，因此构建目录约为 3.5GB。
